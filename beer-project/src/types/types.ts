// Skapa på mina vänner!

export type Beer = {
    id: number,
    name: string,
    description: string,
    food_pairing: string;
    image_url?: string,
    brewers_tips: string
    

    }

    export type Button = {
        text: string;
      };

      export type Input = {
        placeholder:string;
      }